# 核心依赖
flask>=2.3
winotify>=1.1              # Windows Toast 通知（仅 Windows）

# 消息渠道 SDK（按需保留，仅在 hook 等待期间临时加载）
websockets>=12.0           # QQ Bot WebSocket 临时监听
lark-oapi>=1.0.0           # 飞书 Bot WebSocket 临时监听
dingtalk-stream>=0.24.0    # 钉钉 Bot Stream 临时监听
